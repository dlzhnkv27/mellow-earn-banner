# Названия сущностей баннера

- `banner-bg` и `banner-container`: одна и та же сущность (один элемент) размером `1130x200`, с `corner radius 20px`, подготовленная под будущий SVG background.
  - По ширине адаптивный: до `1130px`, при более узком окне сжимается вместе с viewport.
  - Текущий режим фона: SVG `./assets/banner-container-bg.svg`.
  - Текущий SVG фон обновлен из источника `/Users/ilya/Desktop/Новая папка 2/Group 4656.svg`.
  - Внутренняя тень: `inset 0 0 0 1px rgba(255, 255, 255, 0.5)` с `mix-blend-mode: overlay`.
- `earn-vault-container-1`: первый внутренний блок в `banner-container`.
- `earn-vault-container-2`: второй внутренний блок в `banner-container`.
- `earn-vault-container`: общий базовый класс для обоих внутренних блоков.
- `vault-1`: белая плашка внутри `earn-vault-container-1`, прижата к левому краю.
- `vault-2`: белая плашка внутри `earn-vault-container-2`, прижата к левому краю.
- `vault-hitbox`: стабильная зона наведения для `vault`, не анимируется и не меняет свои границы при hover.
- `vault`: общий базовый класс для обеих белых плашек.
- `script.js`: логика интерактивного наклона `vault` от позиции курсора.
- `earn-vault-stats`: правый блок статистики внутри `earn-vault-container`, расположен справа от `vault`.
- `earn-vault-stat-row`: строка статистики внутри `earn-vault-stats`.
- `earn-vault-stat-label`: левая колонка строки статистики.
- `earn-vault-stat-value`: правая колонка строки статистики.
- `vault-shapes`: контейнер шейпов внутри `vault`, выровнен по правому краю.
- `vault-circle-shape`: круглый шейп внутри `vault`, подготовлен под будущий SVG background.
- `vault-pill-shape`: черный шейп в форме пилюли справа от круглого шейпа.
- `vault-pill-label`: подпись внутри `vault-pill-shape`, слева от белого круглого контейнера.
- `vault-pill-icon-container`: белый круглый контейнер внутри `vault-pill-shape`, подготовлен под будущую иконку.

# Ключевые параметры

- Фон страницы: `#101115`.
- Padding у `banner-bg`: слева/справа `40px`, сверху/снизу `34px`.
  - В desktop при сужении окна горизонтальный padding уменьшается заметно и плавно до `24px`; вертикальный padding остается фиксированным.
- Gap между `earn-vault-container-1` и `earn-vault-container-2`: `44px`.
  - В desktop уменьшается заметно и плавно при сжатии окна, вплоть до `20px`.
- Для `earn-vault-container-1/2`:
  - `corner radius: 48px`
  - `padding: 6px`
  - Декоративный слой `::before`: `background-color: rgba(255, 255, 255, 0.05)`
  - Декоративный слой `::before`: `inset 0 0 0 1px rgba(255, 255, 255, 0.1)`
  - Декоративный слой `::before`: `mix-blend-mode: overlay`
  - Реальный внутренний контент: `mix-blend-mode: normal`
- Для `earn-vault-stats`:
  - В desktop-версии расположен справа от `vault`
  - Содержит `2` строки
  - Между строками горизонтальный divider
  - Divider тянется до правого бордера `earn-vault-container`
- Для `earn-vault-stat-row`:
  - `2` колонки без вертикального разделения
  - Отступ слева: `28px`
  - Отступ справа: `42px`
  - Расстояние между левой и правой колонкой уменьшается плавно при сжатии desktop-окна
  - Левая колонка выровнена по левому краю
  - Правая колонка выровнена по правому краю
- Для `earn-vault-stat-label`:
  - Тексты: `TVL`, `APY`
  - `font-family: "Helvetica Neue", Helvetica, Arial, sans-serif`
  - `font-size: 15px`
  - `font-weight: 400`
  - `color: #FFFFFF`
- Для `earn-vault-stat-value`:
  - Тексты: `$25.12M`, `5.34%`
  - `font-family: "Helvetica Neue", Helvetica, Arial, sans-serif`
  - `font-size: 15px`
  - `font-weight: 700`
  - `color: #FFFFFF`
- Для `vault-1/2`:
  - В desktop: фиксированная ширина `300px`
  - В mobile: `width: 50%` от родительского `earn-vault-container`
  - `border-radius: 44px`
  - `padding: 24px`
  - `background-color: #FFFFFF`
  - `box-shadow: 0 3px 12px 0 rgba(0, 0, 0, 0.4)`
  - Hover depth-анимация: `400ms`
  - На hover: плашка приподнимается вверх на `3px`, не увеличивается по масштабу, тень становится больше и интенсивнее
  - На hover: наклон сейчас отключен, максимальное значение установлено в `0deg`
- Для `vault-hitbox`:
  - Ширина совпадает с базовой шириной `vault`
  - В desktop layout левая и правая секции внутри `earn-vault-container` делятся как `50/50`
  - Используется как стабильный hover/pointer-сенсор
  - Нужен для того, чтобы анимация `vault` не вызывала дрожание на пограничной области
- Для `vault-shapes`:
  - Занимает всю внутреннюю ширину `vault`
  - `gap: 12px`
- Для `vault-circle-shape`:
  - Круглая форма
  - Подготовлен под будущий SVG через `background-image`
  - В `vault-1`: `./assets/vault-1-circle-bg.svg`
  - В `vault-2`: `./assets/vault-2-circle-bg.svg`
- Для `vault-pill-shape`:
  - Форма пилюли
  - Занимает всю доступную ширину внутри `vault` за вычетом круга и `gap`
  - Не сжимается меньше ширины своего контента, чтобы фиксированный правый отступ у белого круга не схлопывался визуально
  - `padding-left: 24px`
  - Пространство между подписью и белым круглым контейнером: авто, зависит от текущей ширины `vault`, но минимум `8px` и этот минимум не сжимается
  - `background-color: #000000`
  - На hover по `vault`: круглый блик `72x72` проходит слева направо и уходит за правую границу за `800ms`
  - Стиль блика: горизонтальный линейный градиент, слева `0%`, справа `15%` белого
- Для `vault-pill-label`:
  - В `vault-1`: `EarnETH`
  - В `vault-2`: `EarnUSD`
  - `font-family: "Helvetica Neue", Helvetica, Arial, sans-serif`
  - `font-size: 18px`
  - `font-weight: 700`
  - `color: #FFFFFF`
  - Текст не должен обрезаться или уходить в `ellipsis`
- Для `vault-pill-icon-container`:
  - Диаметр: `32px`
  - Прибит к правому краю внутри `vault-pill-shape`
  - Отступ справа: `20px`
  - Этот правый отступ фиксированный и не меняется между desktop / tablet / mobile
  - Выравнивание: по вертикали по центру
  - `background-color: #FFFFFF`
  - Иконка внутри: `./assets/vault-pill-arrow.svg`
  - Работает как маска для hover-анимации стрелки
  - На hover по `vault`: текущая стрелка уходит вправо, новая приходит слева за `400ms`

# Анимации

- Вне hover у баннера нет постоянных анимаций.
- Общий триггер интерактива: `:hover` на `vault`.
- Технически интерактив привязан к `vault-hitbox`, а анимируется внутренний `vault`, чтобы граница сенсора не двигалась вместе с карточкой.
- У `vault` курсор: `pointer` по всей площади.
- Анимация глубины `vault`:
  - Длительность: `400ms`
  - Тайминг: `cubic-bezier(0.22, 1, 0.36, 1)`
  - Сценарий: `vault` поднимается вверх на `3px`, масштаб остается `1`, тень становится более крупной и насыщенной
- Анимация наклона `vault`:
  - Источник: `/Users/ilya/Documents/Vibe/Codex/Mellow/Banner/script.js`
  - Длительность перехода: `400ms`
  - Максимальный наклон: `0deg`
  - Логика: система расчета наклона сохранена, но при текущем значении `0deg` визуальный наклон отсутствует
  - Направление наклона готово следовать за положением курсора по обеим осям, если значение будет увеличено позже
  - Расчет ведется от стабильной геометрии `vault-hitbox`, а не от трансформированного прямоугольника самого `vault`
  - Обновление наклона ограничено одним кадром через `requestAnimationFrame`
  - При выходе курсора из визуального footprint поднятого `vault` наклон возвращается к `0deg`
- Анимация стрелки в `vault-pill-icon-container`:
  - Длительность: `400ms`
  - Тайминг: `ease-out`
  - Количество циклов: `1`
  - Сценарий: текущая стрелка уходит вправо, новая стрелка приходит слева
  - Белый круг `vault-pill-icon-container` работает как маска за счет `overflow: hidden`
- Анимация блика в `vault-pill-shape`:
  - Статус: временно отключена
  - Длительность: `800ms`
  - Тайминг: `ease-out`
  - Количество циклов: `1`
  - Форма блика: круглый шейп `72x72`
  - Градиент блика: горизонтальный линейный, слева `0%` белого, справа `15%` белого
  - Траектория: стартует у левого края пилюли, проходит слева направо по всей пилюле и полностью уходит за правую границу
  - Конфигурация эффекта сохранена в CSS, на hover запуск сейчас отключен
- Стабилизация hover на границе:
  - Hover не сбрасывается мгновенно в первой пограничной точке
  - Для выхода используется расширенная зона, соответствующая lift и scale карточки
  - Это устраняет цикл `enter -> leave -> enter` на краях приподнятого `vault`
- При `prefers-reduced-motion: reduce` отключаются hover-анимации глубины, наклона, стрелки и блика.

# Адаптив

- Популярные брейкпоинты:
  - `tablet`: `max-width: 1024px` (включая диапазон `768-1024px`)
  - `mobile`: `max-width: 767px`
- Поведение в desktop до `tablet`:
  - Баннер остается в desktop-композиции и сжимается плавно без отдельного desktop-брейкпоинта.
  - Верхние и нижние padding у баннера сохраняются, а горизонтальные уменьшаются плавно.
  - Gap между двумя vault-карточками и расстояние между колонками статистики тоже уменьшаются плавно.
- Поведение на `tablet`:
  - `banner-container` становится `auto` по высоте и подстраивается по ширине в viewport.
  - Внутри каждого `earn-vault-container` layout становится вертикальным.
  - `vault` остается сверху, а `earn-vault-stats` смещается вниз под `vault`.
  - Ширина `earn-vault-stats` совпадает с шириной `vault`.
  - Дополнительная отдельная полупрозрачная подложка у `earn-vault-stats` отключена; остается только общий нижний фон `earn-vault-container`.
  - `earn-vault-stats` отображается как 2 равные колонки.
  - Левая колонка содержит `TVL` и его значение, правая колонка содержит `APY` и его значение.
  - Внутри каждой tablet-колонки label и value стоят вертикально друг под другом и выровнены по центру колонки.
  - Между двумя tablet-колонками проходит вертикальный полупрозрачный divider.
  - Desktop hover/tilt-интерактив и 3D-perspective на tablet отключены для более легкого ресайза и рендера.
- Поведение на `mobile`:
  - Две vault-карточки (`earn-vault-container-1/2`) отображаются вертикально друг под другом.
  - Внутренняя композиция каждой карточки построена на tablet-схеме: `vault` сверху, `earn-vault-stats` снизу.
  - `earn-vault-stats` сохраняет tablet-структуру из 2 равных колонок: `TVL` и `APY` с их значениями, центрированных внутри колонок.
  - Между двумя mobile-колонками сохраняется вертикальный полупрозрачный divider.
  - `banner-container` на mobile использует увеличенный `border-radius: 56px`, чтобы внешний контур органичнее сочетался с внутренними `earn-vault-container` на `48px`.
  - Белые блоки `vault` на mobile уменьшены на `15%` по высоте: с `112px` до `95.2px`.
  - Внутренний `padding` у `vault` на mobile также уменьшен на `15%`: с `24px` до `20.4px`.
  - Правый отступ белого круглого контейнера внутри черной `vault-pill-shape` на mobile вычисляется от высоты pill и равен визуальному свободному полю сверху/снизу; сейчас это `11.2px`.
  - Высота `earn-vault-stats` на mobile уменьшена на `15%`: с `80px` до `68px` (`calc(80px * 0.85)`).
  - Вертикальный padding строк `earn-vault-stats` на mobile уменьшен еще на `15%`: с `12px` до `10.2px` (`calc(12px * 0.85)`).
  - Desktop hover/tilt-интерактив и 3D-perspective на mobile отключены для более плавного ресайза и рендера.
  - На mobile hover-поведение отключено явно: нет pointer-курсора, нет подъема карточки, нет анимации стрелки и нет hover-анимаций pill.
